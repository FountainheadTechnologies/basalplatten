image: node:7

stages:
  - test
  - deploy

before_script:
  - npm install

test:
  except:
    - tags
  script:
    - npm run test
    - npm run build

publish:
  stage: deploy
  only:
    - master
  except:
    - tags
  script:
    - npm run build
    - $(npm bin)/semantic-release-gitlab
